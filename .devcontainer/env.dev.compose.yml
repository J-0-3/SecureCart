# Devcontainer to be run alongside other development compose configurations.
services:
  devcontainer:
    build: 
      context: .devcontainer
      args:
        - DB_HOST=db
        - DB_USERNAME=securecart
        - DB_PASSWORD=securecart
        - DB_DATABASE=securecart
        - REDIS_HOST=redis
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock # Codespaces allows this for some reason